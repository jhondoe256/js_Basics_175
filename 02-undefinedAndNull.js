//Undefined: a value that hasn't been interacted with yet.
let coffieCupContents;

console.log(coffieCupContents); //Prints undefined


//null -> changing an existing value to 'nothing' (dev)
let userData = "User name and Email Here.";
console.log(userData);

//set the variable to null to purposely remove its contents.
userData = null;

console.log(userData);