//* Functions -> a block of code created to perform a specific task.
//* these 'tasks' can be adding numbers together, running a conditional, or printing something to the console

//* Declaration
/*
    Syntax:
      1.       2.  
    function hello(){
                 3.
        console.log("Hello World!");
    }
 */

//* Activating a function -> to execute a function, we 'call' or 'invoke' it
//* [functionName]() -> invokes the function!

function hello() {
  console.log("Hello World");
}

// hello();
// hello();
// hello();
// hello();
// hello();
// hello();
// hello();

for (let i = 1; i <= 7; i++) {
    hello();
}
